# Examples

- `stable-demo-result.json`
  Exported headless run for the `stable` scenario with seed `42`.
- `adaptive-buffer-comparison-demo-result.json`
  Exported headless run for the adaptive comparison preset with seed `42`.

Regenerate the example exports with:

```bash
./scripts/run_demo.sh stable 42
./scripts/run_demo.sh adaptive-buffer-comparison 42
```
