# Resume Materials

## Resume bullets

- Built `MediaFlow Studio`, a C++20 real-time media transport simulator with deterministic seeded replay, modeling sender, RTP-like packetization, unreliable network behavior, receiver sequencing, and a configurable jitter buffer.
- Designed a performance-sensitive simulation engine with fixed-step timing, bandwidth shaping, burst loss, reorder, and queue pressure, then exposed live telemetry through a local HTTP API for interactive monitoring.
- Implemented a portfolio-grade React + TypeScript dashboard with scenario controls, seeded replay, live charts for latency/jitter/loss/throughput/buffer occupancy, and transport event logs for explainable demos.
- Added production-style engineering quality gates with CMake, GoogleTest, CTest, JSON-driven scenario configs, integration-style simulation tests, and complete design/interview documentation.

## LinkedIn-ready summary

Built MediaFlow Studio, an interview-ready C++20 and React project that simulates real-time media transport over unreliable networks. It models RTP-like packets, deterministic seeded replay, jitter buffering, bandwidth pressure, packet loss, and reorder behavior, then visualizes live latency, jitter, throughput, loss, and event traces in a polished monitoring dashboard.

## GitHub repo description

Deterministic C++20 real-time media transport simulator with jitter buffer analytics, scenario-driven impairments, GoogleTest coverage, and a React dashboard.
